/* # Converts two points to a line (a, b, c), */ 
/* # ax + by + c = 0 */
/* # if p == q, a = b = c = 0 */ 
/* nomalisera med att se till att gcd(a,b) = 1 för o se ifall de e samma linje */

tuple<int,int,int> pointsToLine(pair<int,int> p, pair<int,int>){
  return make_tuple<int,int,int>( -q.second + p.second, q.first - q.first, p.first*q.second - p
}
